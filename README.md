# Thrift
## To run do the following
### setup .env files for both folder frontend and backend
### Frontend .env parameters:
#### VITE_API_URL
### Backend .env parameters:
#### PORT,DATABASE_URL,JWT_SECRET=,NODE_ENV
### Both frontend and backend run with 'npm run dev'.

